import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* other config options */

    // output: "export",
  images: {
    domains: ["cdn.couponzguru.com","api.couponsculture.com","api.shopsmaart.com" ,"connect.couponsculture.com" ],
    
  },
};

export default nextConfig;


// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     // output: "export",
// };

// export default nextConfig;


